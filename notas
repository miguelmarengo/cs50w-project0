1. en el directorio que estara el proycto
git init

2. añadir a stage todos los archivos o poner el nombre del archivo a añadir por ejemplo aaa.py
git add -A

3. ver el status
git status

4. commit with message
git commit -m "mi mensaje delmcommit"

5. push this commit files localy to the remote repository
Pushing changes:
git diff
git status
-------
git push origin master
git pull origin master

-------------------------------------------
Common flow
1. create a branch
git branch xxxx
2. para ver cuantas branches tengo
git branch
3. para seleccionar un branch
git checkout nombre_branch
-----------------------------------------
Push a branch to a remote repository
1. push
git push -u https://github.com/me50/miguelmarengo.git web50/projects/2020/x/search

la -u indica que se quedan asociadas ese repository y esa branch con mi direcrotio
en proximas veces solo teclear
git push
ó
git pull



ver que cambios tiene mi codigo del directorio
git diff

ver que no esta en STAGE
git status

add to the staging directory todos los archivos -A, sino x.py
git add -A






https://github.com/me50/USERNAME.git

Instrucciones de git
1. en mi directorio, donde esta el proyecto



git config --global user.name "miguelmarengo"
git config --global user.email "miguelmarengo@arete.ws"

To check the configuration, run:
git config --global --list


git init.defaultbranch web50/projects/2020/x/search

git config --global init.defaultBranch web50/projects/2020/x/search

git branch -m web50/projects/2020/x/search

-----------------------------------------------------
1.
git clone https://github.com/miguelmarengo/cs50w_project0.git

2.
git init

3.
git remote add origin git@gitlab.com:username/cs50w_project0.git